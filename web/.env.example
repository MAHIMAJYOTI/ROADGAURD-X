# Copy to web/.env.local for local dev, or set in Vercel/Netlify for production.
# API base URL (no trailing slash). Omit on localhost if API runs on :8000 on the same machine.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
